<%= simple_form_for(cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit class: "btn btn-primary m-1"%>
<% end %>
<%= link_to 'Back', cocktails_path, class: "btn btn-danger m-1"%>
